const Loader = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        fontSize: "15px",
        height: "100%",
      }}
    >
      Loading....
    </div>
  );
};

export default Loader;
